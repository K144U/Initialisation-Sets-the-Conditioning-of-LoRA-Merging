# Qwen-2.5-7B + magicoder @ alpha=0.0 — E5 Arm 2 pilot.
# Mixed training data pre-baked by gen_e5_pilot_datasets.py.

base_model: /home/sanjay.g/projects/rdmerge/models/Qwen2.5-7B-Instruct

task:
  name: magicoder
  dataset: ise-uiuc/Magicoder-OSS-Instruct-75K
  config: null
  split_train: train
  split_eval: train
  n_train: 7500
  n_eval: 1000
  prompt_field: problem
  answer_field: solution
  train_jsonl: artifacts/e5_pilot_data/qwen25_7b/magicoder__alpha00.jsonl

lora:
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules: [q_proj, k_proj, v_proj, o_proj]

train:
  epochs: 2
  lr: 5.0e-5
  batch_size: 8
  grad_accum: 4
  max_seq_length: 2048
  warmup_ratio: 0.03
  optim: adamw_torch
  logging_steps: 10
  dataset_num_proc: 4
  min_free_gb: 40

output:
  adapter_dir: artifacts/lora/qwen25_7b/e5_pilot/magicoder__alpha00/v1
  results_path: results/phase3/lora_train/qwen25_7b_e5pilot_magicoder_alpha00.json

seeds:
  global: 20260614
  train: 20260614
