% ===================================================================
% REPRODUCIBILITY STATEMENT. Unnumbered, before references.
% Points at the artifacts without breaking double-blind anonymity:
% no author-identifying URLs, no cluster paths. A full decision log
% accompanies the camera-ready (kept internal during review per the
% project's transparency policy).
% ===================================================================

All theoretical claims are stated with explicit assumptions in
\S\ref{sec:setup} and proved in \S\ref{sec:lower}--\S\ref{sec:achv}
and the supplementary material. For the empirical results we
release, on publication, the training and evaluation code, the
exact per-task configuration files (base model, LoRA rank and
target modules, optimizer, token budget, eval split sizes), the
random seeds, and the per-example negative-log-likelihood arrays
underlying every cell of the merge matrix.

The merge matrix comprises $40$ cells ($4$ base models $\times$
$\{$Task Arithmetic, TIES, DARE, KnOTS$\}$ plus a six-rate TVQ
sweep). Each cell loads the shared frozen base and four
task-specific LoRA adapters, applies one merging method with
uniform weights, and reports worst-task excess NLL on $n=1000$
held-out examples per task. We verified that training and
evaluation splits are disjoint (the held-out slice is taken
after a single shuffle of the source split, with a separate
audit confirming zero overlap). Confidence intervals are $95\%$
percentile bootstrap over $10{,}000$ resamples of the
per-example NLL; method-to-method comparisons use a paired
bootstrap in which the shared per-task baseline cancels.
Point estimates were reproduced across two independent
evaluation runs and agreed to within $0.001$ nats/token; we
report the second run, which additionally logs per-example
values. Results use a single training seed, and we are explicit
that the reported intervals quantify evaluation uncertainty
rather than seed variance.

A chronological log of design decisions, intermediate negative
results, and a data-handling correction made during the study
accompanies the camera-ready version, in the interest of full
methodological transparency.

\paragraph{Per-experiment manifest.}
Table~\ref{tab:repro-manifest} enumerates every experiment in the
paper with the count of evaluation cells, the script used to
reproduce it from the released artifacts, and the per-cell wallclock
on a single NVIDIA A100-80GB. ``Cells'' counts independent
$(\text{base}, \text{method}, \text{$T$}, \alpha)$ tuples actually
evaluated.

\begin{table}[h]
\centering
\caption{Experiment manifest. Each row lists the section reporting
the experiment, the count of evaluation cells, the script in the
released code that reproduces it (under \texttt{code/phase3/scripts}),
and approximate per-cell wallclock on a single A100-80GB. All
training uses Unsloth+PEFT with bfloat16; evaluation uses
\texttt{PYTHONNOUSERSITE=1}.}
\label{tab:repro-manifest}
\small
\begin{tabular}{lrll}
\hline
Experiment (\S) & cells & reproducer script & cell wallclock \\
\hline
E1 multi-seed matrix (\S\ref{subsec:exp-multiseed})       & $40$ & \texttt{run\_eval\_cell.py} configs/eval\_matrix\_seeds & 12--18 min \\
E5 floor-positive null (\S\ref{subsec:exp-e5-null})      & $12$ & \texttt{run\_eval\_cell.py} configs/eval\_e5\_arm3       & 10--14 min \\
E7 b=2 mechanism (\S\ref{subsec:exp-b2-mechanism})       & $24$ & \texttt{run\_eval\_cell.py} configs/eval\_e7\_prune      & 12--16 min \\
E3 GSM8K (\S\ref{subsec:exp-downstream-gsm8k})           & $20$ & \texttt{run\_downstream\_cell.py} configs/eval\_e3\_gsm8k & 18--25 min \\
B4 HumanEval pass@1 (\S\ref{subsec:exp-downstream-gsm8k})& $20$ & \texttt{run\_downstream\_cell.py} configs/eval\_b4\_humaneval & 10--20 min \\
B2 chat-template probe (\S\ref{subsec:exp-downstream-gsm8k}) & $5$ & \texttt{run\_downstream\_cell.py} configs/eval\_b2\_l3\_chat & 8--10 min \\
E6 $T$-scaling Yi (\S\ref{subsec:exp-T-scaling})        & $54$ & orchestrator + configs/eval\_e6\_pilot                 & 8--12 min \\
E6 $T$-scaling Llama (\S\ref{subsec:exp-T-scaling})     & $54$ & orchestrator + configs/eval\_e6\_pilot                 & 8--12 min \\
TIES sign-election probe (\S\ref{subsec:exp-T-scaling}) & $2$ & \texttt{probe\_ties\_sign\_election.py} (CPU only)        & 10 min CPU \\
E10 baselines (\S\ref{subsec:exp-e10})                  & $8$ & orchestrator + configs/eval\_e10\_baselines           & 10--15 min \\
E11 quadratic-bridge (\S\ref{subsec:exp-e11})           & $16$ & orchestrator + configs/eval\_e11\_quadbridge          & 8--12 min \\
\hline
\end{tabular}
\end{table}

The total compute footprint of the empirical results is
approximately $80$--$120$ GPU-hours on a single A100-80GB, plus the
LoRA training of the $16+12$ adapters at $T = 4$ and the $6$ pilot
adapters at $T = 7$ (each $7500$ training examples, $\sim 25$ min on
the same hardware).
