# Local Mirror Manifest

Snapshot of the JIIT-cluster project `/home/sanjay.g/projects/rdmerge/`
fetched to this local working directory on **2026-06-30**, for replication and
as submission proofs. The cluster repo is **163 GB**; this mirror deliberately
omits ~140 GB of large, regenerable, non-proof data (see *Excluded* below).

## Mirrored locally (the proofs + working tree)
- `results/` — all experiment result JSONs (per-example NLL arrays, worst-task
  excess, method kwargs, env). The core empirical evidence.
- `code/` — training/eval/merge code, **all configs**, orchestrator, analyzers,
  PBS scripts (including this session's sweep generators + analyzers).
- `artifacts/lora/` — **trained LoRA adapters** (4 bases × 4 tasks × seed1/2/3+v1),
  22 GB, 178 adapters. Already present locally from the original
  `rdmerge_complete_2026-06-27.tar` extraction (verified intact 2026-06-30: no
  truncated/0-byte files; `seed1/2/3` and `v1` variants all present). The
  irreplaceable trained artifacts (would otherwise require GPU re-training); not
  re-downloaded this session.
- `artifacts/e5_pilot_data/`, `logs/`, `notes/`, `theory/`, `paper_artifacts/`,
  `.git/` (full history/provenance), and all top-level `*.md` design docs.
- `paper/` and `overleaf/` — **local copies are authoritative** (they carry this
  session's edits) and were intentionally NOT overwritten by the cluster pull.

## Excluded (regenerable / not proofs / too large for local disk)
- `models/` (**111 GB**) — base model weights. Standard public HF downloads, not
  proofs. Re-obtain from Hugging Face:
  - `meta-llama/Llama-3.1-8B-Instruct`
  - `mistralai/Mistral-7B-Instruct-v0.3`
  - `Qwen/Qwen2.5-7B-Instruct`
  - `01-ai/Yi-1.5-9B-Chat`
  (Config `base_model:` paths point at `<repo>/models/<dir>`; recreate that
  layout or repoint the configs.)
- `cache/` (22 GB) — Hugging Face dataset/model cache, regenerated on first run.
- `.conda/` (8.2 GB) — Linux conda env; rebuild on the target machine from
  `requirements.txt`. Not usable on Windows directly.
- `unsloth_compiled_cache/` — regenerated by Unsloth at runtime.

## This session's new experiments (2026-06-29/30), all included
- **Density sweep** (`results/phase3/eval_density_sweep/`, 32 cells) — TIES/DARE
  worst-task excess vs density {0.05,0.1,0.3,0.5} (+0.2 reused). Summary:
  `results/phase3/density_sweep_summary.json`. Analyzer: `analyze_density_sweep.py`.
- **RegMean ridge-λ sweep** (`results/phase3/eval_regmean_lambda/`, 20 cells) —
  λ ∈ {1e-4,1e-2,1e-1,1,10} (+1e-3 reused). Summary:
  `regmean_lambda_summary.json`. Analyzer: `analyze_regmean_lambda.py`.
- **3-seed apples-to-apples rd-ridge vs tuned-RegMean** on Qwen+Yi
  (`results/phase3/eval_seed_rdridge_regmean/`) — on matched seed1/2/3 adapters
  (motivated by the `v1`≠`seed1` adapter-set mismatch found this session).
  Analyzer: `analyze_seed_rdridge_regmean.py`. *Pulled after job 43138 finishes.*

## Re-sync note
`results/phase3/eval_seed_rdridge_regmean/` and the seed summary are pulled once
the in-flight 3-seed job (`43138`) completes; re-run the `results/` tar-pull to
refresh.
